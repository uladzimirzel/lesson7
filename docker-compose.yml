---
version: '3'
services:
   package-maven:
      build:
         context: .
         dockerfile: Dockerfile
      volumes:
        - build-maven:/usr/local/tomcat/webapps
   start-tomcat:
      #image: tomcat:9.0-alpine
      ports:
        - 8080:8080
      depends_on:
        - package-maven
      volumes:
        - build-maven:/usr/local/tomcat/webapps

volumes:
  build-maven:
...